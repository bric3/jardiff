# Native image configuration for jardiff application
# This module uses picocli for CLI

# Picocli - CLI framework, initialize at build time for faster startup
Args = --initialize-at-build-time=picocli